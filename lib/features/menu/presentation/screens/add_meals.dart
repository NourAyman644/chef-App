import 'package:flutter/material.dart';

class AddMeals extends StatelessWidget {
  const AddMeals({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
